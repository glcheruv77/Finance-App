# AI Finance Manager — React + Node (Plaid Sandbox)

## What this is
A local sandbox-ready project combining:
- React frontend (client/)
- Node.js + Express backend (server/) with Plaid sandbox integration

## Quick setup

1. Clone or create the folder and files from README.
2. Install dependencies (root will install both server & client):
   ```bash
   npm install